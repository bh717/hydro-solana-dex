name: example-app-build
env:
  cache-id: example-app-20220427b
  CARGO_TERM_COLOR: always
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - "*"

jobs:
  build-example-app-deps:
    runs-on: ubuntu-latest
    steps:
      - id: cache-cli-deps
        name: cache-cli-deps
        uses: actions/cache@v2
        with:
          key: $${{ env.cache-id }}
          path: |
            ~/.local/share/solana
            ~/.cargo
            ~/.rustup
            ~/.cargo/bin/anchor
            ~/.cargo/bin/wasm-pack

      - id: checkout
        name: checkout
        uses: actions/checkout@v2
        with:
          fetch-depth: 0

      - id: install-os-deps
        name: install-os-deps
        if: steps.cache-cli-deps.outputs.cache-hit != 'true'
        run: sudo apt-get update && sudo apt-get install -y pkg-config build-essential libudev-dev tree make

      - id: install-rust-toolchain
        name: install-rust-toolchain
        if: steps.cache-cli-deps.outputs.cache-hit != 'true'
        uses: actions-rs/toolchain@v1
        with:
          toolchain: 1.60.0

      - run: echo "PATH=$HOME/.local/share/solana/install/active_release/bin:$PATH" >> $GITHUB_ENV

      - id: install-solana-tools
        name: install-solana-tools
        if: steps.cache-cli-deps.outputs.cache-hit != 'true'
        run: make install_solana

      - run: echo "PATH=$HOME/.local/share/solana/install/active_release/bin:$PATH" >> $GITHUB_ENV

      - id: install-anchor-cli
        name: install-anchor-cli
        if: steps.cache-cli-deps.outputs.cache-hit != 'true'
        run: make install_anchor

      - id: install-node
        name: install-node
        uses: actions/setup-node@v2
        with:
          node-version: "16"

      - id: install-yarn
        name: install-yarn
        run: npm install -g yarn

      - id: install-serve
        name: install-serve
        run: npm install -g serve

  build-example-app:
    runs-on: ubuntu-latest
    needs: [build-example-app-deps]
    steps:
      - id: cache-cli-deps
        name: cache-cli-deps
        uses: actions/cache@v2
        with:
          key: $${{ env.cache-id }}
          path: |
            ~/.local/share/solana
            ~/.cargo
            ~/.rustup
            ~/.cargo/bin/anchor
            ~/.cargo/bin/wasm-pack

      - run: echo "PATH=$HOME/.local/share/solana/install/active_release/bin:$HOME/.cargo/bin:$PATH" >> $GITHUB_ENV

      - uses: actions/checkout@v2
        with:
          fetch-depth: 0

      - id: install-wasm-pack
        name: install-wasm-pack
        run: install_wasm_pack

      - id: install-wasm-target
        name: install-wasm-target
        run: rustup target add wasm32-unknown-unknown

      # https://dev.to/mpocock1/how-to-cache-nodemodules-in-github-actions-with-yarn-24eh
      - id: cache-node-modules
        name: cache-node-modules
        uses: actions/cache@v2
        with:
          path: "**/node_modules"
          key: ${{ runner.os }}-modules-${{ hashFiles('**/yarn.lock') }}

      - id: cache-turbo-folders
        name: cache-turbo-folders
        uses: actions/cache@v2
        with:
          path: "**/.turbo"
          key: ${{ runner.os }}-turbo-files

      - id: install-ipfs-deploy
        name: install-ipfs-deploy
        run: npm install -g ipfs-deploy

      - run: make example-app-build
        name: example-app-build

      - name: example-app-build - Notify slask success
        if: success()
        env:
          SLACK_BOT_TOKEN: ${{ secrets.SLACK_NOTIFICATIONS_BOT_TOKEN }}
        uses: voxmedia/github-action-slack-notify-build@v1
        with:
          channel: gitops
          status: EXAMPLE-APP-BUILD-SUCCESS
          color: good

      - name: example-app-build - Notify slack failure
        if: failure()
        env:
          SLACK_BOT_TOKEN: ${{ secrets.SLACK_NOTIFICATIONS_BOT_TOKEN }}
        uses: voxmedia/github-action-slack-notify-build@v1
        with:
          channel: gitops
          status: EXAMPLE-APP-BUILD-FAILED
          color: danger

      - id: ipfs-deploy
        name: ipfs-deploy
        uses: aquiladev/ipfs-action@v0.2.1
        with:
          service: infura
          path: ./examples/sdk/build

      - run: echo ${{steps.ipfs-deploy.outputs.cid}}

      - name: example-app-deploy - Notify slask success
        if: success()
        env:
          SLACK_BOT_TOKEN: ${{ secrets.SLACK_NOTIFICATIONS_BOT_TOKEN }}
        uses: voxmedia/github-action-slack-notify-build@v1
        with:
          channel: gitops
          status: EXAMPLE-APP-CD-SUCCESS https://ipfs.infura.io/ipfs/${{steps.ipfs-deploy.outputs.cid}}
          color: good

      - name: example-app-deploy - Notify slack failure
        if: failure()
        env:
          SLACK_BOT_TOKEN: ${{ secrets.SLACK_NOTIFICATIONS_BOT_TOKEN }}
        uses: voxmedia/github-action-slack-notify-build@v1
        with:
          channel: gitops
          status: EXAMPLE-APP-CD-FAILED
          color: danger
